<p>
<div class="row">
  <div class="col-sm-12 text-end">
    <% if edit %>
        <button id="activate-tip" class='btn btn-sm btn-success' disabled><%=t(".activate")%></button>
        <button id="deactivate-tip"class='btn btn-sm btn-danger' disabled><%=t(".deactivate")%></button>
        <%= link_to new_tablesetting_path, class: 'btn btn-sm btn-primary' do %>
            <i class="bi bi-plus"></i> <%=t(".tip")%>
        <% end %>
    <% else %>
        <button id="activate-tip" class='btn btn-sm btn-success' disabled><%=t(".activate")%></button>
        <button id="deactivate-tip"class='btn btn-sm btn-danger' disabled><%=t(".deactivate")%></button>
        <%= link_to new_tip_path, class: 'btn btn-sm btn-primary disabled' do %>
            <i class="bi bi-plus"></i> <%=t(".tip")%>
        <% end %>
    <% end %>
  </div>
</div>
<div style="padding-top:10px" class="table-borderless" id="restaurant-tip-table">
</div>