<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card">
        <div class="card-header">
          <h3 class="h5 mb-0">Import Menu from PDF</h3>
          <p class="text-muted mb-0 small">Upload a PDF menu to automatically extract menu items and sections.</p>
        </div>
        
        <div class="card-body">
          <%= form_with(model: [@restaurant, @ocr_menu_import], local: true, html: { class: 'needs-validation', novalidate: true }) do |f| %>
            <div class="mb-4">
              <%= f.label :name, 'Menu Name', class: 'form-label' %>
              <%= f.text_field :name, 
                  class: 'form-control', 
                  placeholder: 'e.g., Dinner Menu, Lunch Specials, etc.',
                  required: true %>
              <div class="invalid-feedback">
                Please provide a name for this menu.
              </div>
            </div>

            <div class="mb-4">
              <%= f.label :pdf_file, 'PDF File', class: 'form-label' %>
              <div class="border rounded p-4 text-center">
                <div class="mb-3">
                  <i class="bi bi-file-earmark-pdf text-muted" style="font-size: 3rem;"></i>
                </div>
                <div class="mb-2">
                  <label for="file-upload" class="btn btn-outline-primary btn-sm">
                    <i class="bi bi-upload me-1"></i> Choose File
                    <%= f.file_field :pdf_file, 
                        id: 'file-upload', 
                        class: 'd-none', 
                        accept: 'application/pdf', 
                        required: true,
                        onchange: "document.getElementById('file-name').textContent = this.files[0]?.name || 'No file chosen'" %>
                  </label>
                  <div id="file-name" class="small text-muted mt-2">No file chosen</div>
                </div>
                <p class="small text-muted mb-0">PDF up to 10MB</p>
              </div>
              <div class="invalid-feedback">
                Please select a PDF file to upload.
              </div>
            </div>

            <div class="d-flex justify-content-end gap-2 pt-3">
              <%= link_to 'Cancel', restaurant_ocr_menu_imports_path(@restaurant), class: 'btn btn-outline-secondary' %>
              <button type="submit" class="btn btn-primary">
                <i class="bi bi-upload me-1"></i> Upload & Process
              </button>
            </div>
          <% end %>
        </div>
      </div>
    
      <div class="card mt-4">
        <div class="card-header">
          <h3 class="h5 mb-0">Tips for Best Results</h3>
        </div>
        <div class="card-body p-0">
          <dl class="list-group list-group-flush">
            <div class="list-group-item">
              <dt class="fw-bold">File Quality</dt>
              <dd class="mb-0 text-muted">
                Use high-quality PDFs with clear, readable text. Avoid handwritten menus or low-resolution scans.
              </dd>
            </div>
            <div class="list-group-item">
              <dt class="fw-bold">Format</dt>
              <dd class="mb-0 text-muted">
                The system works best with digital PDFs. Scanned documents may have lower accuracy.
              </dd>
            </div>
            <div class="list-group-item">
              <dt class="fw-bold">Processing Time</dt>
              <dd class="mb-0 text-muted">
                Larger files may take several minutes to process. You'll receive an email when processing is complete.
              </dd>
            </div>
          </dl>
        </div>
      </div>
    </div>
  </div>
</div>
