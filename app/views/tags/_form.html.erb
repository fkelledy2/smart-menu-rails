<%= form_with(model: tag) do |form| %>
  <% if tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :description, class: 'form-label' %>
      <%= form.text_area :description, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :menuitem_id, class: 'form-label' %>
      <%= form.text_field :menuitem_id, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <% if tag.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', tag, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if tag.persisted? %>
      <%= link_to "Cancel", tag, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", tags_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
