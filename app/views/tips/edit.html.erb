<div class="page-header">
  <div style='padding-top: 20px' class="row">
    <div class="col-10">
        <h2><%= t(".tip") %></h2>
    </div>
    <div class="col-2">
        <% if @tip.persisted? %>
          <%= link_to t(".cancel"), edit_restaurant_path(id: @tip.restaurant.id), class: "float-end btn btn-light" %>
        <% else %>
          <%= link_to t(".cancel"), edit_restaurant_path(id: @tip.restaurant.id), class: "float-end btn btn-light" %>
        <% end %>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-12">
    <%= render 'form', tip: @tip %>
  </div>
</div>
