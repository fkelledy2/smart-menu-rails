<% content_for :title, 'Edit Vendor Cost — Admin' %>
<div class="container-fluid py-4">
  <%= render 'shared/admin_breadcrumb',
        crumbs: [{ label: 'Admin', url: admin_vendor_costs_path },
                 { label: 'Vendor Costs', url: admin_vendor_costs_path }],
        current: 'Edit' %>
  <h1 class="h3 mb-1">Edit Vendor Cost Entry</h1>
  <%= render 'form', cost: @cost %>
</div>
