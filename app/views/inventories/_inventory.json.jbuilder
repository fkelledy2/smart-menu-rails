  json.id inventory.id
  json.startinginventory inventory.startinginventory
  json.currentinventory inventory.currentinventory
  json.resethour inventory.resethour
  json.menuitem inventory.menuitem
  json.created_at inventory.created_at
  json.updated_at inventory.updated_at
  json.url inventory_url(inventory, format: :json)
