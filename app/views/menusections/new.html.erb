<div class="page-header">
  <div class="row">
    <div class="col-sm-10">
          <h1>New Menu Section</h1>
    </div>
    <div class="col-sm-2">
        <% if @menusection.persisted? %>
          <%= link_to "Cancel", @menusection, class: "float-end btn btn-light" %>
        <% else %>
          <%= link_to "Cancel", menusections_path, class: "float-end btn btn-light" %>
        <% end %>
    </div>
  </div>
</div>
<%= render 'form', menusection: @menusection %>