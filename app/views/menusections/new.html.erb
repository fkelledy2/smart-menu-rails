<div class="page-header">
  <div class="row">
    <div class="col-sm-10">
        <h1><%= t(".menuSection") %></h1>
    </div>
    <div class="col-sm-2">
        <% if @menusection.persisted? %>
          <%= link_to t(".cancel"), @menusection, class: "float-end btn btn-light" %>
        <% else %>
          <%= link_to t(".cancel"), menusections_path, class: "float-end btn btn-light" %>
        <% end %>
    </div>
  </div>
</div>
<%= render 'form', menusection: @menusection %>