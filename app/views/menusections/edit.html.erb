<div class="page-header">
  <div style='padding-top: 20px' class="row">
    <div class="col-sm-9">
          <h1>Edit Menu Section</h1>
    </div>
    <div class="col-sm-2">
        <% if @menusection.persisted? %>
          <%= link_to "Cancel", edit_menu_path(@menusection.menu), class: 'float-end btn btn-light' %>
        <% else %>
          <%= link_to "Cancel", edit_menu_path(@menusection.menu), class: 'float-end btn btn-light' %>
        <% end %>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-sm-11">
    <%= render 'form', menusection: @menusection %>  </div>
</div>

