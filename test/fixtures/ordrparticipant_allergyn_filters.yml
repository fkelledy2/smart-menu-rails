# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# No fixtures defined - this prevents FK constraint violations with system tests
# Model tests should create their own test data instead of relying on fixtures
