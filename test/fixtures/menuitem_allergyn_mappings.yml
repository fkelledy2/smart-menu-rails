# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# Fixtures removed to prevent foreign key constraint violations during test cleanup
# Tests requiring menuitem_allergyn_mappings should create their own data
